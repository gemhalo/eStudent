<%= form_for(@academic_calander) do |f| %>
  <% if @academic_calander.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@academic_calander.errors.count, "error") %> prohibited this academic_calander from being saved:</h2>

      <ul>
      <% @academic_calander.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :start_date %><br />
    <%= f.date_select :start_date %>
  </div>
  <div class="field">
    <%= f.label :end_date %><br />
    <%= f.date_select :end_date %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
