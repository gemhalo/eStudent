<%= form_for(@department) do |f| %>
  <% if @department.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@department.errors.count, "error") %> prohibited this department from being saved:</h2>

      <ul>
      <% @department.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :college_id %><br />
     <%= f.collection_select(:college_id,@colleges,:id,:name) %>
  </div>
  <div class="field">
    <%= f.label :dept_head %><br />
    <%= f.text_field :dept_head %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
