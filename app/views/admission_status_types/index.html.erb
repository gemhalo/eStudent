<h1>Listing admission_status_types</h1>

<table>
  <tr>
    <th>Name</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @admission_status_types.each do |admission_status_type| %>
  <tr>
    <td><%= admission_status_type.name %></td>
    <td><%= link_to 'Show', admission_status_type %></td>
    <td><%= link_to 'Edit', edit_admission_status_type_path(admission_status_type) %></td>
    <td><%= link_to 'Destroy', admission_status_type, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Admission status type', new_admission_status_type_path %>
