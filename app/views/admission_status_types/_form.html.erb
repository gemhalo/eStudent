<%= form_for(@admission_status_type) do |f| %>
  <% if @admission_status_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@admission_status_type.errors.count, "error") %> prohibited this admission_status_type from being saved:</h2>

      <ul>
      <% @admission_status_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
