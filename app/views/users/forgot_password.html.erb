
<%= flash[:notice] %>

<legend>Forgot Password</legend>
<%= form_for :users, :url => { :action => 'forgot_password' }  do |f| %>

  Email:<%= f.text_field :email, :size => 20 %>
	<%= f.submit('Submit')%>
<%end%>
