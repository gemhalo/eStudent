<%= flash[:notice] %>


<%= netzke :people,
           :class_name => "Basepack::GridPanel",
           :model => 'Person',
           :columns => [
           :name, :father_name, :grand_father_name],
           :scope=> {:id.gteq => @person.first.id},
           :width => 650
           
          %>
<%= @person.inspect %>
  <%= link_to 'create accounts', :controller => 'moe_data_import', :action => 'show', :id => @person %>
