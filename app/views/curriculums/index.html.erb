<h1>Listing curriculums</h1>

<table>
  <tr>
    <th>Course</th>
    <th>Program</th>
    <th>Course type</th>
    <th>Semester</th>
    <th>Class year</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @curriculums.each do |curriculum| %>
  <tr>
    <td><%= curriculum.course_id %></td>
    <td><%= curriculum.program_id %></td>
    <td><%= curriculum.course_type %></td>
    <td><%= curriculum.semester_id %></td>
    <td><%= curriculum.class_year_id %></td>
    <td><%= link_to 'Show', curriculum %></td>
    <td><%= link_to 'Edit', edit_curriculum_path(curriculum) %></td>
    <td><%= link_to 'Destroy', curriculum, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Curriculum', new_curriculum_path %>
